# Stakick Browser Watcher

This service opens Kick channels in a persistent Chromium profile.

It does not store Kick passwords or OAuth credentials.

## Requirements

- Node.js 20+
- Chromium-compatible Playwright browser
- A normal Kick account

## Install

```bash
npm install
npx playwright install chromium
