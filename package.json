{
  "name": "stakick-bot",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute stakick-db --remote --file=./schema.sql",
    "deploy:full": "wrangler deploy",
    "setup:webhook": "node scripts/setup-webhook.mjs",
    "bootstrap": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
